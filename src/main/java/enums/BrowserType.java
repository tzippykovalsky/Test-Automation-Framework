package enums;

/**
 * BrowserType enum represents different types of web browsers
 * that can be used for running WebDriver tests.
 */
public enum BrowserType {
    CHROME,
    FIREFOX,
    EDGE,
    SAFARI,
    OPERA,
    IE,           // Internet Explorer
    HEADLESS      // Headless browser mode
}
